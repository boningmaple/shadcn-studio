// Loads the stylesheet. The suite asserts on the accessible name of the
// theme switch, and which of its labels reaches the accessibility tree is
// decided entirely by CSS: without it all three are exposed at once.
import "@/styles.css";
